{
  "name": "spearcode-desktop",
  "private": true,
  "version": "0.2.0",
  "description": "SpearCode as a real windowed desktop app — hosts the existing Ink TUI in a Tauri 2 window via a PTY + xterm.js.",
  "type": "module",
  "scripts": {
    "web:dev": "vite",
    "web:build": "vite build",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon src-tauri/icons/source.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0"
  }
}
